---
- name: Provision services/asg (node exporter + app)
  hosts: "role_{{ target_role | default('service') }}"
  become: true

  roles:
    - docker
    - node_exporter
    # 앱도 같이 올릴 거면 아래 role 하나 더(선택)
    # - app_stack
