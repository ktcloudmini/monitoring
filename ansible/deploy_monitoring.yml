---
- name: Deploy Prometheus + Grafana on monitoring instance
  hosts: monitoring
  become: true
  become_method: sudo
  become_flags: "-n"
  roles:
    - common
    - deploy_stack
    - prometheus_config
  vars:
    compose_profiles_csv: "monitoring"
